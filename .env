# Database variables  
DB_HOST='soundmachine'
DB_USER='root'
DB_PASSWORD='irenchit'
DB_NAME='SoundMachinedb'
DB_PORT="27017"
DATABASE_URL=`postgresql://${DB_USER}:${DB_PASSWORD}@${HOSTNAME}:${DB_PORT}/${DB_NAME}`;

# Server variables  
BACKEND_PORT=3000;
HOSTNAME="localhost";
SERVER_URL=`https:localhost:${BACKEND_PORT}`
